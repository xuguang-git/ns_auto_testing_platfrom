DEBUG=true
SECRET_KEY=replace-with-a-long-random-secret
ALLOWED_HOSTS=127.0.0.1,localhost,192.168.5.88
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://192.168.5.88:5173

DB_NAME=ns_auto_test
DB_USER=root
DB_PASSWORD=Admin123@
DB_HOST=127.0.0.1
DB_PORT=3306

# Redis config is reserved and can be replaced after infrastructure is confirmed.
REDIS_URL=redis://127.0.0.1:6379/0

# Test data database connection example. Use a read-only database account.
# Database management reads this list and shows connections as read-only rows.
TEST_DATA_DB_CONNECTIONS=TEST_ORDER_DB
# TEST_ORDER_DB_ENVIRONMENT=test
# TEST_ORDER_DB_DISPLAY_NAME=Order test readonly DB
# TEST_ORDER_DB_ENABLED=true
# TEST_ORDER_DB_TYPE=postgresql
# TEST_ORDER_DB_HOST=127.0.0.1
# TEST_ORDER_DB_PORT=5432
# TEST_ORDER_DB_NAME=order_test
# TEST_ORDER_DB_USER=readonly_user
# TEST_ORDER_DB_PASSWORD=replace-with-readonly-password
